Plain HTML and CSS only: no JavaScript, no build tools, unless I explicitly ask for them.
Keep index.html at the repo root; all asset paths stay relative (e.g. style.css, not /style.css).
Reuse existing classes and spacing conventions in style.css instead of adding new ones for a similar element.
Ask before adding any new file, dependency, or script tag.
Leave DECISIONS.md and verification/ alone — those are mine to write.